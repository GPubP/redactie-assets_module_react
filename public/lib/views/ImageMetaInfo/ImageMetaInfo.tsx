import React, { FC } from 'react';

import { ImageMetaForm } from '../../components';

const ImageMetaInfo: FC = () => {
	return <ImageMetaForm onSubmit={() => null} />;
};

export default ImageMetaInfo;
